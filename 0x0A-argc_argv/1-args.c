#include <stdio.h>

/**
 * main - entry point
 * 	  prints number of arguments passed in
 * 	  and exits
 * Return: Always(0);
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
